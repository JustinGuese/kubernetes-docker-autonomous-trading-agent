# ─── LLM ────────────────────────────────────────────────────────
OPENROUTER_API_KEY=sk-...                  # Your OpenRouter API key (paste manually, never commit)

# ─── Solana ─────────────────────────────────────────────────────
SOLANA_PRIVATE_KEY=<base58-64-byte-keypair> # Devnet keypair (base58 encoded, 64 bytes)
SOLANA_RPC_URL=https://api.devnet.solana.com

# ─── GitHub (self-modification push) ───────────────────────────
GITHUB_TOKEN=ghp_...                       # Personal access token (repo scope)
GITHUB_REPO=owner/repo                     # e.g. jguese/aiAutonomousTraderBot
GITHUB_BRANCH=main

# selenium
BROWSER_CDP_URL=http://localhost:3000

# ─── Policy caps ────────────────────────────────────────────────
CONFIDENCE_THRESHOLD=0.6                   # Minimum LLM confidence to execute an action
MAX_SOL_PER_TX=0.1                         # Hard cap per transaction (SOL)
DAILY_SPEND_CAP_SOL=0.5                    # Rolling daily cap (SOL)
MAX_LOC_DELTA=200                          # Max lines-of-code change per self-modification
