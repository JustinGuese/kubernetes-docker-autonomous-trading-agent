apiVersion: batch/v1
kind: CronJob
metadata:
  name: aiautonomoustraderbot
  namespace: autonomoustrading
spec:
  schedule: "3 17 * * *" # daily at 9 AM UTC
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid # never run two instances simultaneously
  jobTemplate:
    spec:
      backoffLimit: 0 # run-once-and-exit: no retries
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: trader-bot
            image: guestros/aiautonomoustraderbot:latest
            imagePullPolicy: Always
            envFrom:
            - secretRef:
                name: trader-bot-env
            env:
            - name: BROWSER_CDP_URL
              value: "http://browserless-chrome:9222"
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: 25m
                memory: 25Mi
            volumeMounts:
            # agent memory — PVC mounted as directory, file managed by app
            - name: memory-pvc
              mountPath: /app/memory
          volumes:
          - name: memory-pvc
            persistentVolumeClaim:
              claimName: trader-bot-memory

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trader-bot-memory
  namespace: autonomoustrading
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
